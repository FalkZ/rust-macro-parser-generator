import math from "./math";
import { op } from "./std";

const _ = {};

function fn(a, b) {
  return op(a, math["+"], b);
}

function one(b) {
  return console.log(b);
}

export function zero() {
  return op(a, math["+"], b);
}

export default { zero };
//# sourceMappingURL=file.m1n.map
